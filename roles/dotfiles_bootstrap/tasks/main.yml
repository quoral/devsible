- name: "Clone Dotfiles repository"
  git:
    repo: '{{ dotfiles_repository }}'
    dest: "{{ playbook_dir }}/roles/dotfiles"
    version: "{{ dotfiles_branch }}"

- name: "Include role"
  include_role:
    name: dotfiles
